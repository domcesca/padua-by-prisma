# Census Data API key, used only by the ETL (python -m hcai_etl acs-county) to fetch county
# demographics. The app itself needs no environment variables.
# Free key: https://api.census.gov/data/key_signup.html
CENSUS_API_KEY=
